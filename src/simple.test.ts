describe("Tests setup check", () => {
  test("1 + 2 should equal 3", () => {
    expect(1 + 2).toBe(3);
  });

  test("basic multiplication works", () => {
    expect(2 * 3).toBe(6);
  });

  test("basic subtraction works", () => {
    expect(5 - 2).toBe(3);
  });
});
